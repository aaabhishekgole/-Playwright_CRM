# Copilot Instructions

- Follow the Pages -> Modules -> Tests structure.
- Do not import page objects directly inside specs.
- Keep locators inside page classes as arrow functions.
- Keep workflow logic inside modules.
- Prefer fixtures and shared test data over hard-coded duplication.
- Use `test.step()` in every spec for report clarity.
